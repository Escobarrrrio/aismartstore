## What this changes

## Why

## Testing
- [ ] `npm test` passes locally
- [ ] `npx tsc --build --force` passes locally (not `tsc --noEmit` -- the root tsconfig is solution-style and that checks zero files)
- [ ] Tested manually in the browser (describe what you checked)

## Database changes
- [ ] This PR includes a Supabase migration (linked/described below)
- [ ] No database changes
